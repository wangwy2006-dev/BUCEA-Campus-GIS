# BUCEA Campus GIS — Project Guide

## 1. 项目定位

北京建筑大学大兴校区 GIS 是一个面向教学、展示与后续数字孪生实验的轻量级 Web GIS 项目。

核心目标不是追求测绘级精细建模，而是在统一地理坐标框架下，逐步形成三个相互关联但用途不同的场景：

1. **校园二维 GIS**：地图、校园边界、建筑、道路、绿地、操场、POI、查询与专题表达。
2. **校园三维 GIS**：与二维 GIS 共用同一套空间数据，通过 Cesium 的二三维模式和三维表达形成统一校园场景。
3. **开心农场虚拟实验场景**：不作为二维或三维 GIS 的普通图层，而作为独立的虚拟场景入口；后续逐步接入模拟传感器、气象站、IoT 与软硬协同机制。

总体空间叙事：

> 全球数字地球 → 中国 → 北京 → 大兴 → 北京建筑大学大兴校区 → 校园二维/三维 GIS → 开心农场虚拟实验场景

---

## 2. 技术路线（默认长期约束）

保持极简、可公开部署、易理解、低维护成本：

- **CesiumJS**
- **HTML**
- **CSS**
- **JavaScript**
- **GitHub Pages**

在 `PROJECT.md` 未明确修改之前，不引入：

- ArcGIS
- Leaflet
- Vite
- Node.js
- 后端服务
- 数据库
- Docker

如后续确有必要引入新的技术栈，应先在本文件中说明原因、收益、替代方案和迁移成本，再实施。

---

## 3. 二维与三维一体化原则

二维校园 GIS 和三维校园 GIS 不是两套独立系统。

它们应遵循：

- 共用同一套校园空间数据；
- 共用统一坐标；
- 共用实体标识和属性；
- Cesium 负责 2D / 3D 场景切换；
- 同一建筑、道路、绿地等对象，在二维中以平面地图方式表达，在三维中以高度、模型或三维符号表达；
- 避免重复维护两份校园数据。

---

## 4. 开心农场定位

开心农场是校园项目的第三条独立主线，定位为：

> **虚实融合的校园智慧农场实验场景**

早期阶段以虚拟表现和交互为主，不追求真实硬件接入。

建议演进顺序：

1. 虚拟农田、道路、作物、温室、小屋等基础场景；
2. 模拟温度、湿度、土壤湿度、光照、风速、降雨等数据；
3. 虚拟气象站与传感器可视化；
4. 统一传感器数据接口；
5. ESP32 / 实际传感器接入；
6. 实时数据驱动虚拟场景；
7. 灌溉等设备的虚实协同；
8. AI / Agent 辅助分析与决策。

真实硬件接入前，应保证纯虚拟版本可以独立运行和演示。

---

## 5. 当前阶段

当前已完成：

- CesiumJS 最小可运行页面；
- OpenStreetMap 底图；
- 校园概略定位；
- 全球数字地球入口；
- 自动从较大尺度飞向校园；
- “全球 / 校园 / 二维 / 三维 / 开心农场”五个入口；
- 开心农场概略位置和独立入口。

当前尚未完成：

- 校园边界数据；
- 校园建筑真实轮廓；
- 道路、绿地、操场等校园专题数据；
- 建筑高度与三维表达；
- POI 与查询；
- 开心农场虚拟场景主体；
- 模拟传感器和气象站。

---

## 6. 近期开发顺序

坚持“小步、可见、可回退”。优先顺序：

1. 校园边界；
2. 校园建筑轮廓；
3. 道路与主要空间结构；
4. 绿地、操场等基础专题；
5. 二维与三维共用数据验证；
6. 建筑基础高度表达；
7. POI / 简单信息查询；
8. 开心农场基础虚拟场景；
9. 模拟传感器；
10. 气象站与虚实协同接口。

如果公开数据质量不足，应优先采用“概略、可解释、可替换”的占位数据，不伪装成精确测绘成果。

---

## 7. 每日推进规则

每日只推进 **一个小而明确、低风险、可独立验收的任务**。

原则：

- 不同时改多个核心功能；
- 优先修改已有文件和复用已有结构；
- 不为了“工程化”而增加技术栈；
- 每次提交都应可独立运行；
- 出现错误时优先回退到最近稳定版本；
- 没有实际 GitHub 提交，不称为“已完成”；
- 每次提交后记录今天完成、可验收内容和下一步。

---

## 8. 与课程的关系

校园项目是两门课程的贯穿案例，但课程本身以理论与技术体系为主，不写成项目操作手册。

### 《三维 GIS》

校园案例主要用于说明：

- 数字地球与多尺度空间表达；
- 坐标与空间数据；
- 二维 / 三维一体化；
- 三维数据模型；
- WebGL、瓦片、3D Tiles；
- 三维查询、分析与交互；
- 数字孪生。

### 《人工智能与空间信息》

校园和开心农场主要用于说明：

- 时空数据；
- 遥感与视觉感知；
- 时空预测；
- 空间语义与知识；
- 大模型 Tool Use / GIS Skill；
- IoT 与实时数据；
- GeoAgent 与 Spatial Intelligence。

课程目标目前按 **两门课程各 8 次课、每次 90 分钟**维护；每讲最终形成约 **28–35 页 PPT**，并配套 **90 分钟教师讲稿**。课程具体结构以私有仓库 `wangwy2006-dev/GIS-Courses` 的 `README.md` 为准。

---

## 9. 验收标准

一个功能只有同时满足以下条件，才视为当前阶段完成：

- 浏览器可直接打开；
- GitHub Pages 可部署；
- 不破坏已有入口；
- 二维与三维数据逻辑保持一致；
- 功能可用一句话解释；
- 可用于课程截图或课堂演示；
- 没有引入当前阶段不必要的依赖。
